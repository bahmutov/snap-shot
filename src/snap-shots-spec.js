'use strict'

/* global describe, it */
describe('snap-shots', () => {
  const snapShots = require('.')
  it('write this test', () => {
    console.assert(snapShots, 'should export something')
  })
})
